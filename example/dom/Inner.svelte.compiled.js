/* ./Inner.svelte generated by Svelte v__VERSION__ */
import {
	SvelteComponent,
	append,
	attr,
	detach,
	element,
	init,
	insert,
	listen,
	noop,
	null_to_empty,
	safe_not_equal,
	text
} from "https://esm.sh/svelte/internal";

function add_css() {
	var style = element("style");
	style.id = "svelte-d602q6-style";
	style.textContent = ".purple.svelte-d602q6{background-color:purple}";
	append(document.head, style);
}

function create_fragment(ctx) {
	let div;
	let t;
	let div_class_value;
	let mounted;
	let dispose;

	return {
		c() {
			div = element("div");
			t = text("Inner");
			attr(div, "class", div_class_value = "" + (null_to_empty(color) + " svelte-d602q6"));
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, t);

			if (!mounted) {
				dispose = listen(div, "click", /*click_handler*/ ctx[0]);
				mounted = true;
			}
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div);
			mounted = false;
			dispose();
		}
	};
}

let color = "purple";

function instance($$self) {
	const click_handler = () => alert(color);
	return [click_handler];
}

class Example extends SvelteComponent {
	constructor(options) {
		super();
		if (!document.getElementById("svelte-d602q6-style")) add_css();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default Example;